package com.app.packageone;

public class ChildClassOne extends ParentClassOne {
	
	public ChildClassOne()
	{
		System.out.println("Constructor of ChildClassOne");
	}
	/*public static void main(String[] args) {
		ChildClassOne c=new ChildClassOne();
		c.display();
		 p1.display(); 
	 	Calling public method from a subclass in same package.
		It displayed (ParentClassOneDisplay)Value of x is: 10 
		
		
		 * p1.display();
		 * Calling private method from a subclass in same package
		 * Error encountered: The method display() from the type 
		 *		ParentClassOne is not visible
		 
		
		 p1.display(); 
	 	Calling protected method from a subclass in same package.
		It displayed (ParentClassOneDisplay)Value of x is: 10 
		
		 p1.display(); 
	 	Calling default method from a subclass in same package.
		It displayed (ParentClassOneDisplay)Value of x is: 10 
	}*/
}
